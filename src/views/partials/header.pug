header
    a(href="/").header__logo
        i.fab.fa-youtube
        span WeTube
    nav
        ul
            li
                a(href="/search")
                    i.fas.fa-search
            if loggedIn
                li
                    a(href="/videos/upload")
                        i.fas.fa-video
                li
                    a(href="/users/edit")
                        i.fas.fa-pen-to-square
                li
                    a(href="/users/logout")
                        span.logout__btn LOG OUT
                li
                    a(href=`/users/${loggedInUser._id}`)
                        if !loggedInUser.avatarUrl
                            span 🙂
                        else
                            if loggedInUser.avatarUrl.includes("github")
                                img.header__avatar(src=loggedInUser.avatarUrl)
                            else
                                img.header__avatar(src="/" + loggedInUser.avatarUrl)

            else
                li
                    a(href="/login").login__btn  Login                        
                li
                    a(href="/join").header__btn  Join